services:
  web-admin:
    image: ghcr.io/aboutnb/github-action-web-admin:latest
    container_name: web-admin
    restart: unless-stopped
    ports:
      - "18700:18700"
    env_file:
      - ./backend/.env
